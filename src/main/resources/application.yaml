spring:
  application:
    name: Tome

  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

aladin:
  ttb-key: ${ALADIN_API_KEY}